[build]
builder = "nixpacks"

[deploy]
startCommand = "python data/generate.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
